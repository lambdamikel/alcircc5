# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Repository Is

This is a **description logic research project** investigating the decidability of concept satisfiability in ALCI_RCC5 and ALCI_RCC8, open problems from Wessel (2002/2003). The repository contains papers (LaTeX), implementations (Python), and documentation. The work was produced by AI assistants (Claude/Anthropic and GPT-5.4/OpenAI), prompted by Michael Wessel (miacwess@gmail.com, GitHub: lambdamikel).

The results are **unverified** and published as a discussion piece for the DL community.

## Repository Structure

- `papers/` -- All LaTeX sources, PDFs, and paper subdirectories (trees/, gpt/, review*/)
- `src/` -- All Python implementations (reasoners, tests, verification scripts)
- Root -- README.md, CONVERSATION.md, CLAUDE.md, OUTDATED.md

## Build

Compile papers:
```
cd papers
pdflatex -interaction=nonstopmode decidability_ALCIRCC5.tex
pdflatex -interaction=nonstopmode decidability_ALCIRCC5.tex  # second pass for references
```

Run tests:
```
cd src
python3 cover_tree_tableau.py          # cover-tree tableau (built-in tests)
python3 stress_test_cover_tree.py      # cross-validation (911 concepts)
python3 decomposition_test.py          # cover-tree decomposition test
python3 model_verifier.py             # independent model verification
python3 alcircc5_reasoner.py          # quasimodel reasoner (built-in tests)
python3 gis_taxonomy.py               # GIS taxonomy (18 concepts, ~190s)
```

Two LaTeX passes are needed to resolve `\ref` and `\cite` cross-references. The bibliography uses `thebibliography` (inline), not BibTeX.

## Current Decidability Argument

**Status (2026-07-12, later): strongly supported, NOT certified. The current *candidate* statement is the round-12/14 semantic layer (cold-certified by the 9th review) + Claude's round-15 decision layer `papers/fable5_round15/cluster_quasimodels_round15.tex` (cluster quasimodels — NO automaton; sole external input = RCC5 patchwork; the Vardi dependency is gone). Round-15 is reviewer-authored and UNREVIEWED (designated weak points W1 steering induction, W2 horizontal normal form, W3 one-step saturation; machine checks WP26); presume it contains defect #10. The paragraph below records the superseded 9th-review-era status.**

**Status (2026-07-12): strongly supported, NOT certified. The current *candidate* statement is round-12 (base) + GPT-5.5's round-14 active-virtual-bag repair (`papers/gpt5.5_round14/split_forest_round14_repaired_proof.tex`), with round-13 §8's width accounting as a surviving (and now itself faulted) component. Round-14 WAS cold-reviewed (9th review, fresh Fable 5, `papers/cold_review_round14/referee_report_packet{1,2,3}*.pdf`): verdict GAP — the AV semantic half (amalgamation Thm 3.3, coverage, truth Thm 4.3, strong equality) was reconstructed and found sound ("the strongest versions of these results in the stack's history"), but Theorem C is UNPROVED for the AV language and the round-14 §6 monitor sketch is UNSOUND as described (F1, critical): the enrichment `N*` lives off the tape, the automaton must decide an existential-projection language, independent alternating run copies cannot be forced to guess consistently (machine-checked acceptance-without-validity configuration: rows pin `e` to {PP} via one port and {DR} via another, intersection empty), and all three natural repairs (tape annotation / profile quotient / canonical guessing) are blocked by facts the stack itself establishes. Plus F2 (D4 request-source liveness load-bearing for truth but absent from width accounting) and F3 (round-14 §1 declares round-13 §§3–8 "replaced" yet quotes §8's `K_A(C₀)`; round-13 Lemma 8.2's recurrence does not close — same-rank (g2) cascades; fix: rank := remaining modal depth). July cycle history: 7th review (hot, Claude) found round-12's Shadow Amalgamation gap → Claude's round-13 (horizontal discipline) → 8th review (GPT-5.5, targeted) broke literal D2 on width (C_G2a tower) → GPT-5.5's round-14 (active virtual bags) → 9th review (cold, above). Score: 9 reviews, 9 defects (seven cold, two hot/targeted; the 9th report's own header says "eighth" — it counted from packet-visible history). The keystone is now THE DECISION LAYER: prove Theorem C as a projection with a coherence-forcing device (bounded-interface/uniformization lemma), or replace the tree-automaton layer with a mosaic/type-elimination decision procedure on the infinitary AV abstraction (whose model-construction half the review certifies as done). Round-15 task = the review's R1–R6.** The decidability theorem is plausible and the split-forest *semantic normal form* (witness thinning → generated containment covers → split copies → side-interface mosaics → typed-EQ quotient = **Theorem A**) has been repeatedly found sound. What no manuscript has yet done is survive a review without a new defect (**7 reviews, 7 defects** — six cold, the seventh hot/context-loaded). The repaired automata proof's **Theorem B** (finite abstraction adequacy) was cold-reviewed by a fresh, genuinely cold Opus 4.8 — a different model lineage from GPT-5.5, who authored it (`papers/automata_route_repairs/referee_report_ALCI_RCC5.md`) — and found *not proved*: its interface-transformer machinery derives a globally consistent labelling of an infinite tree from purely local checks, and the global coherence is asserted, not proved. GPT-5.5's **round-12 repair** took the reviewer's recommended route — delete the bespoke transformer layer; rest global consistency on the **RCC5 patchwork property** + a **two-way parity automaton**. The **canonical statement is now `papers/automata_route_repairs/split_forest_automata_with_appendices.tex`** (29pp — the round-12 *expanded self-contained edition*: same A/B/C theorem spine as the 14pp `split_forest_patchwork_automata_repaired.tex`, plus 21 appendices A–U discharging every load-bearing step, incl. the finite→countable patchwork passage by **compactness over the finite relation alphabet** with **no route-dependent transformer**). The 5th-review repair (`split_forest_automata_repaired_full_proof` + the A/B/C companion) is the predecessor, and the original 21-page automata proof is superseded. The expanded edition is **not a new repair round** (no new defect was found — the author was asked to make the existing round-12 self-contained) and is **not itself cold-reviewed**. **Do not describe the theorem as proven or certified, and do not describe round-12 (in either the 14pp or 29pp form) as cold-reviewed — it has not been.**

**Why the switch (the no-automata thread converged onto an automaton).** Across rounds 2–10 the no-automata route repeatedly reduced to automaton-shaped machinery it could not fully discharge: request-closed cycles ≈ Büchi/parity acceptance; "finite product-state exhaustion" ≈ product-automaton reachability. The round-9 proof (`papers/gpt5.5_round3/`) was cold-reviewed by a fresh GPT-5.5 session (via the `papers/cold_review_round9/` packet); the referee report (`papers/gpt5.5_round4/referee_response_alci_rcc5.tex`) found, with machine-checked composition arithmetic:
   - **Critical 1 — one-sided verticalization.** Round-9 handled a DR/PO witness forced into an infinite *ancestor* PPI-tower but missed the dual *descendant* PP-tower. Witness `C = (∃PO.A) ⊓ (∃PPI.⊤) ⊓ ∀PPI.(∃PPI.⊤ ⊓ ∀DR.¬A ⊓ ∀PO.¬A)` (machine-checked in `verification/python/wp12_round10_dual_descendant_verticalization.py`).
   - **Critical 2 — finite-checkability never proved.** V6/V7 were *asserted* to be finite syntactic checks but quantify over the infinite unfolding; the finite catalogues were never shown to exhaust it.
   - **Critical 3 — boundary non-determinacy.** Boundary descriptors do not determine mixed outside/internal labels (`i PP b, o PO b ⟹ L(o,i) ∈ {DR,PO,PPI}`).
   - Plus four majors including a *false* simple-cycle bound (a request-closed cycle may need a closed walk that revisits descriptors).

   Round-10 (`papers/gpt5.5_round4/`) accepts all findings and repairs them: dual splice, occurrence-local requests, ranked side-width, global-quotient-first extraction, a corrected `(M+1)N` closed-walk bound, and — for Critical 2/3 — **finite product-state exhaustion** and **complete interface descriptors**. A deep read of `expanded_split_forest_full_details_round10_merged.tex` found the entire decidability burden now funnels into exactly two named lemmas (Finite Product Exhaustion, Complete-Interface Replacement), both proved by case-list + "the alphabets are finite" + "the descriptor prescribes it" — i.e. asserted, not discharged, and both automaton-shaped. The convergent move is therefore to use the automaton directly.

**The automata proof keeps the split-forest model idea; it only replaces the brittle half.** Per its own §"Why this proof keeps the split-forest idea": *"the automaton does not replace the split forest. It consumes it."* It retains witness thinning, generated containment covers, split copies, side-interface mosaics, and typed-EQ quotient (the sound semantic normal form, shared verbatim with the no-automata thread), and uses a parity tree automaton over the finite split-forest profile alphabet — decided by **non-emptiness** — only for "the parts that were most brittle in a hand certificate: regular infinite branches, simultaneous eventuality fulfillment, recursive side contexts, and profile repetition without semantic equality." Parity-tree-automaton non-emptiness is decidable by construction, so the finite-checkability and eventuality keystones the no-automata route could not discharge come for free.

**The 2026-05-29 promotion was strategic, not a proof upgrade — and the automata route was then cold-reviewed (5th cold review) and had the same gap.** A fresh GPT-5.5 session reviewed it (`papers/automata_route_repairs/rcc5_split_forest_referee_report.tex`) and found that the automata route's `Patch` invariant — that a finite *local* check guarantees *global* RCC5 composition + universal propagation to all composition-forced targets + equality congruence — was **assumed, not constructed**, and that the "automaton" was prose, not a total finite transition function. Concrete UNSAT concepts it would wrongly accept: `C_force = ∃PP.(∃DR.A) ⊓ ∀DR.¬A` (`x PP y, y DR z ⟹ x DR z` forced via `comp(PP,DR)={DR}`, so `∀DR.¬A` kills `z:A`), `C_split = ∃PP.(A ⊓ ¬B ⊓ ∀PP.¬B ⊓ ∀PPI.¬B ⊓ ∀PO.¬B) ⊓ ∃PP.B`, `C_recursive = ∃DR.(A ⊓ ∃DR.B)`. Composition table re-verified correct; witness thinning sound. So the automata route had the no-automata route's keystone gap all along.

**GPT-5.5's repair, and the convergence point.** GPT-5.5 accepted all findings and produced a repaired proof + an **A/B/C companion paper**. The central fix is **finite pair/triple product-state representatives** (computed from nearest-common-interface transformers) — the *same* convergent idea as round-10's product-state exhaustion in the no-automata route. The ABC paper isolates the argument into **Theorem A** (split-forest normal form — shared, found sound), **Theorem B** (finite abstraction adequacy: finite frontier/pair/triple/equality-port abstractions capture saturated split forests up to satisfiability of all closure formulas), and **Theorem C** (a finite alternating parity tree automaton accepts exactly the valid abstractions). **Theorem B is the single load-bearing keystone — and it is the same keystone both routes converged on** (round-10's "finite product-state exhaustion" / "complete interface descriptors" are Theorem B in the no-automata packaging). So the project has cleanly *identified* the one hard theorem; whether it is *proved* is the open question. GPT-5.5 itself flags the product-state frontier construction and the equality-congruence proof as "the sections most deserving of independent review or mechanization." Theorem B was then cold-reviewed in turn (6th cold review; next).

**The 6th cold review (Theorem B) and round-12 (patchwork).** A fresh, genuinely cold Opus 4.8 — a different model lineage from GPT-5.5, who authored the proof — reviewed Theorem B via the `papers/cold_review_theoremB/` packet (`papers/automata_route_repairs/referee_report_ALCI_RCC5.md`). Verdict: **genuine gap; theorem most likely true; repairable**. The keystone (Lemma 6.9, finite pair/triple representation) derives a globally consistent RCC5 labelling of an *infinite* tree from purely *local* interface-transformer checks; the required **confluence law** (initialise-at-ancestor-then-push-both-endpoints-down = initialise-at-the-LCA) is asserted, never checked (C1) — so `ρ_T` may be route-dependent and the read-off labelling need not satisfy composition. The same gap recurs in the multi-parent split `C_split` (C2: `y,z` are roots of different trees, linked only by a deep equality port, so `comp(PPI,PP)`'s DR-exclusion is never enforced through a common interface) and in equality-port congruence (C3: a global ∀ζ condition reduced to finite frontier agreement with no separating-frontier argument); plus M1–M4 (counting-heuristic width bound, asserted shadow finiteness, one-way-automaton sufficiency, a lossy universal may-analysis). The reviewer could *not* build an accepted-but-unsatisfiable witness (so it did not certify the construction *unsound*), re-verified the composition table + witness thinning + every diagnostic-concept verdict as correct, and recommended: **stop hand-re-deriving the patchwork property — cite it** (RCC5 has it), use a **two-way** automaton (Vardi), and a separating-frontier lemma (RCC5 homogeneity).

**Round-12** (`split_forest_patchwork_automata_repaired.tex`, 14pp) takes exactly that route: it **deletes the transformer layer** and rebuilds on a **patchwork-bag abstraction** (finite bags = complete atomic RCC5 networks, adjacent bags agree on overlaps, global consistency via the **RCC5 patchwork property**), a **two-way alternating parity tree automaton** (Vardi emptiness), **exact tree-decomposition identity** for EQ (no quotienting of blocking copies), and **exact relation-vector shadows** for universals (no may-analysis). This is real progress: the unproved *bespoke* keystone (transformer coherence) is replaced by a **standard, citable theorem the project has used and computationally stress-tested all along** — the RCC5 patchwork property (Renz–Nebel; `verification/python/mosaic_closure_search.py` = WP6). Remaining mathematical dependence localizes to two *standard external* results (RCC5 patchwork property; two-way parity-automaton emptiness, Vardi 1998), so round-12 is **not yet fully self-contained**, and the new (smaller) keystone is **patchwork-bag adequacy** — that the saturated split-forest presentation is faithfully captured by finite patchwork bags. Round-12 is **not cold-reviewed in turn**. Trajectory: convergent onto standard tools (the reviewer judged the theorem reachable by standard techniques), but status stays *strongly supported, not certified*.

**The 7th review (2026-07-08, hot audit by Claude Fable 5) and the Shadow Amalgamation gap.** With the newly available Fable 5 (Anthropic, Mythos-class — a tier above Opus), the frozen round-12 manuscript was audited adversarially (`papers/fable5_review/referee_report_round12_fable5.tex/.pdf`, 9pp). This was a **hot** (context-loaded) review, *not* a cold one — it does not discharge the outstanding cold-review requirement — by a different model lineage than the author. Verdict: **genuine gap; theorem still most likely true; repair localized.** Critical finding **F1**: Theorem B(b)'s Truth Lemma builds `ρ_T` by patchwork over the **bag networks only** (App E), then *asserts* the declared relation-vector shadow entries agree with `ρ_T` ("the exact relation vector … includes ρ_T(x,y)=R"); the implicit **Shadow Amalgamation Lemma** (bags + all declared shadow rows jointly realizable in one frame) is unproved and **false for the legality tests as written** — WP15 exhibits a minimal 3-bag configuration (declared `x₁–x₂: DR`, `x₁–p: PP`, `x₂–p: PPI`; `comp(PP,PP)={PP}` forces `x₁ PP x₂`) passing every listed test with **no** realizing frame (23/64 assignments on the pattern fail). This is the 6th review's C1 recurring one layer up (shadow tier instead of transformer tier) but strictly smaller: single-shadow is provably fine (the bag+row family is an agreeing tree family), and no counterexample exists under the **vertical-liveness repair discipline** (PP/PPI/EQ pairs co-bagged; shadow entries DR/PO-only — saturation (S3)/(V7) extended to the shadow layer; 358/358 clean) or the extended-bag discipline (200/200). Also found: two spec loopholes, each soundness-fatal as literally written, each a one-line fix — **F2** (Def 3.3 permits EQ-valued shadow entries, silencing obligations) and **F3** (shadow propagation scope never pinned down; App H's "must continue" is undefined); WP16 shows either loophole flips `C_force`/`C_split`/`C_2hop`/`C_3hop` to ACCEPT, while the repaired discipline matches the cover-tree tableau on all ten diagnostics (incl. **SAT for the strong-EQ PO-loop** — the quasimodel oracle's blind spot, handled correctly by round-12's exact occurrence identity) plus a 148-concept random sweep. **F4**: the `K(C₀)` finiteness proof still asserts "this is finite" for an a-priori-infinite enumeration (6th review's M1, softened). What held up: the patchwork theorem in exactly the round-12 form (WP14: 63,219 two-bag amalgamations + 300 App-A tree patchings, zero failures), the compactness passage (modulo an "a/the completion" wording fix), strong equality, running intersection, and the forest→tree packaging. **No accepted-but-unsatisfiable concept was found** (ALCI's lack of nominals appears to block concept-level exploitation). Keystone now: **the Shadow Amalgamation Lemma + K(C₀) accounting**. The report's §6 is the repair program for a prospective round 13. Response bundle for GPT-5.5: `papers/fable5_review/fable5_review_round12_bundle.zip`.

**Round-13 (2026-07-08, later): the Shadow Amalgamation repair — reviewer-authored, unreviewed.** The 7th reviewer executed its own repair program: `papers/fable5_round13/split_forest_shadow_amalgamation_round13.tex/.pdf` (10pp, a **delta** on round-12 — everything not amended is inherited). Content: the **D-discipline** (D1 horizontal-only shadow entries, vertical/EQ pairs co-bagged = saturation (S3)/(S4)/(V7) extended to the shadow layer; D2 mandatory omni-directional propagation with verticalization fallback; D3 type-propagation of transitive vertical universals, sound by `comp(PP,PP)={PP}`; D4 request-source liveness; **D5 pair coherence** — one declared value per occurrence pair, a rule whose necessity WP17's own development demonstrated), then the **Shadow Amalgamation Lemma**: bags + declared rows + DR-default completion are jointly realizable, proved by a case analysis over **four exhaustively machine-checked composition facts** (horizontal absorption `{DR,PO} ⊆ comp(a,b)` for horizontal a,b; vertical transitivity; no horizontal dead ends; EQ never forced) + the WP14-verified patchwork/compactness apparatus applied to the shadow-extended family. The repaired Truth Lemma then holds with declared entries literally equal to `ρ_T` (the round-11/12 failure mode is closed by construction). **The residual burden moves explicitly to the completeness side**: the Coverage Lemma (every obligation pair is co-bagged, row-covered, or vertical-chain-mediated) and the verticalization-vs-width interaction (round-9/10 territory), plus the repaired **witness-generated `K(C₀)` accounting** (F4 fix: generation reasons g1–g5, computable per-rank recurrence — replaces round-12's "this is finite" assertion). Round-13's own §"what would invalidate" designates G2 (extraction within the discipline) and G3 (Coverage walk) as the next review targets. **Do not treat round-13 as reviewed or its lemmas as certified; the 7-reviews-7-defects presumption applies to it in full.**

**The 8th review and round-14 (2026-07-09, GPT-5.5): literal D2 falls; the active-virtual-bag repair.** GPT-5.5 ran the G2/G3 task packet and broke round-13 exactly at G2a (`papers/gpt5.5_round14/round13_g2_g3_referee_report.tex/.pdf`): for `C_G2a = ∃PP.⊤ ⊓ ∀PP.(∃PP.⊤ ⊓ (∀PP.∃PP.⊤ ⊓ ∀DR.A))` — satisfiable; every tower level carries the horizontal obligation ∀DR.A — literal D2 (omni-directional propagation + verticalization fallback) forces every remote level `a_k` into every lower edge bag (the rigid entry to the separator is the vertical `PPI`, so the fallback fires all the way down): root-bag width m+1 for every prefix m, machine-checked (`wp18`). Also G3b: a closed mixed path `x PP z, z PPI y, x PP y` shows vertical endpoint relations need no uniformly oriented chain, so D3 misses them (`wp19`). The Shadow Amalgamation Lemma itself survived (GPT re-ran WP14/16/17 clean). **Round-14** (`split_forest_round14_repaired_proof.tex`, 12pp) withdraws D1's horizontal-only restriction, D2's fallback, and the DR-default, and adopts the **extended-bag route** (the 7th review's repair option (b), corroborated by WP15-P5): every universal source is a **virtual active occurrence at every bag** (`A_t = P_t ∪ Univ_T`); validity (AV1–AV6) demands a coherent complete atomic relation on each active set whose **every finite restriction** is composition-closed, adjacent active networks agree, obligations are checked against declared entries (AV4), and no default exists for inactive pairs. Amalgamation = finite tree patchwork + compactness on the active family (declared = realized verbatim); **coverage trivializes** (a universal source is active wherever the target is live — no walks, no orientation cases); **extraction is width-free** (virtual entries are not ports; the G2a tower is absorbed with 2 live ports + m−1 virtual shadows, `wp25`). Claude reproduced all seven new harnesses (wp18–wp25) and hot-audited the manuscript: the semantic core (amalgamation/coverage/truth/extraction) is sound as far as hand-checking reaches; **designated review targets for the cold review**: (i) the automaton's monitor bookkeeping — single/pair/triple alternating states enforcing AV1, esp. the spawning discipline for two sources introduced in disjoint subtrees (GPT self-flags this as "the review target"); (ii) the single-shadow transition must check composition closure of the vector with the bag network, not only AV4 (implied by AV1, not spelled out); (iii) the inherited `K_A(C₀)` bound still rests on round-13 §8's witness-generated accounting. Package provenance: `papers/round13_g2_g3_review_package.zip`, `papers/round14_repaired_proof_package.zip`; the repair package also contains an intermediate profile-compression design's checks (`wp21`/`wp22` docstrings) that the final manuscript replaced by per-instance monitors — the facts they check remain true and are kept as regression checks.

**The 9th review (2026-07-12, COLD, fresh Fable 5): the decision layer is the gap.** Three report packets + an independent verification script (`papers/cold_review_round14/referee_report_packet1_verdict.pdf`, `..._packet2_major_findings.pdf`, `..._packet3_verified_appendix.pdf`, `verify_rcc5.py` + output — reproduced locally, byte-identical). **F1 (critical, conf ~0.9):** a valid AV abstraction is a pair (tree `T`, enrichment `N*`); `N*` (relations from every universal source to every port and between universal sources) is not on the tape, so Theorem C must decide the projection `{T : ∃N* (T,N*) valid}` — never stated, never proved; and the §6 monitor sketch is unsound: alternating run copies guess independently, and in the two-port configuration with rows `x–p: PP, y–p: PPI, x–q: PP, y–q: DR` every sketched monitor passes its local checks (the pair monitor simply guesses different private row copies, all-DR) while `e = ρ(x,y)` is pinned to `Comp(PP,PP) ∩ Comp(PP,DR) = {PP} ∩ {DR} = ∅` — acceptance without validity. Repair routes analyzed and blocked: (a) tape annotation — per-node row data is unbounded (round-14's own G2a table: m−1 virtual shadows at one bag); (b) profile quotient — provably lossy (`ρ(a₁,a₃)=PP` vs `ρ(a₅,a₃)=PPI` at identical (τ,Ω) profiles); (c) canonical guessing — needs an unproved uniformization. Discharge options (F1.5): (i) bounded-interface lemma (boundedly many "relevant" rows per bag + existence lemma for the rest — a round-13-style argument compatible with vertical rows); (ii) bounded annotation via a *proven* coalescing congruence; (iii) drop the tree automaton for a mosaic/type-elimination decision procedure on the infinitary AV abstraction (Thms 3.3/4.3 already provide the model-construction half). **F2 (major):** D4 request-source liveness is load-bearing for the truth lemma (deferred existentials discharge only as live bag facts for non-universal sources) but never enters the width accounting; probe p4 (a strong-EQ-guarded globally unique A-witness shared by an infinite tower's ∃PPI.A requests) shows the presentation must extend the *witness's* trace, not the sources' — nothing legislates that choice. **F3 (major):** scope contradiction (round-14 §1 "replacement for round-13 §§3–8" vs Theorem 5.1 quoting §8's `K_A`) + round-13 Lemma 8.2's recurrence does not close (its (g2) witnesses are themselves "already generated" ports spawning same-rank cascades; the one-sentence finite-chain claim bounds chain *finiteness*, not same-rank *length*; fix: a single decreasing measure, rank := remaining modal depth, making (g2) rank-decreasing). **Minors F4–F7** (Lemma 3.2 statement/use mismatch — DR-isolation repair verified; Thm 3.3's `C_F` under-specified — exact rewording supplied; dangling round-12 App H.2 citation; WP scripts not shipped in-stack). **Found sound (S1–S10):** composition table (fresh 6-element derivation), Facts 2.1–2.4, the AV semantic layer *reconstructed* (incl. two holes the text glossed: inactive-occurrence completion via DR-isolation; bidirectional AV4 coverage), extraction at set level, strong equality end-to-end (probes p4/p5 — semantic uniqueness without syntactic identity handled correctly both directions), per-copy parity design, the withdrawal audit (complete except automaton + §8), patchwork in the small (21,521 instances), G2a absorption. **Meta:** the cold review found what both hot audits missed (the unsoundness proof, the scope contradiction, the Lemma 8.2 regress) — third consecutive demonstration that cold > hot on this project. The report self-titles "eighth review" (it counted from packet-visible history); the project ledger counts it as the ninth. Round-15 = R1–R6: Theorem C as projection with proved coherence-forcing, or the mosaic route; bounded-delay/width lemma; §8 erratum + re-proof under a single decreasing measure; one consolidated manuscript; ship scripts.

**Round-15 (2026-07-12, Claude Fable 5): cluster quasimodels — the decision layer without automata (UNREVIEWED).** Executes the 9th review's R2 alternative (`papers/fable5_round15/cluster_quasimodels_round15.tex/.pdf`, 10pp, compiles clean). Certificate = **cluster quasimodel**: types + safe links (with transitive vertical propagation = compute_safe as a link condition) + a finite catalogue of cluster patterns (≤ K(C₀) members, closed atomic, safe-decorated) with **one-step demand fulfilment** (Q2: in-pattern or adjacent-pattern witnesses; no promissory witnesses ⟹ no parity condition needed; the lap-collapse trap is structurally excluded) + gluing coherence (Q3) + **fold conditions** (Q4) over the new **fold lattice** — the closure of the RCC5 atoms under set-composition has exactly **10 reachable sets**, every one a vertical singleton or containing a horizontal atom (WP26 A, exhaustive; the canonical selector forced-value/DR/PO is total) — + **core clusters** for uniquely realizable types (Q5: types with empty self-safe-set get one global realization threaded as a permanent interface — absorbs the 9th review's p4 probe with no per-source liveness cost, dissolving F2) + width (Q6) re-proved under a **single strictly decreasing measure** (remaining modal depth at (g2) witness generation; fixed budgets for boundary/stratum insertions — resolves F3). **Soundness**: unfolding → skeleton (patchwork on the tree family) → **canonical completion** (Lemma: forced values by chain-singletons, free values by horizontal selection within Safe — coherence constructed in the proof, not guessed by a device; the 9th review's F1 is structurally moot) → truth lemma (every pair co-patterned or canonically assigned within Safe — no third case). **Completeness**: extraction from Theorem A presentations; the dichotomy "horizontally presentable or chain-confined" is Lemma hnf = designated **W2**. Keystone = **W1** (the steering induction's two-fresh-member/old-detour fold-algebra cases — WP26 B: 250/250 random glue steps realizable, negative control 245/249). Decidability = finite certificate space + finitely checkable conditions; **no complexity claim**. WP26 part C: 11/11 diagnostics vs tableau (incl. C_G2a, p4 tower) + 200-concept sweep, 0 disagreements. **Do not describe round-15 as reviewed; the 9-reviews-9-defects presumption applies. Next: GPT-5.5 adversarial review targeting W1–W3.**


**Verification.** WP1 + the SAT/UNSAT oracles + WP7–WP9 (Theorem A stress) + **WP6 (patchwork-property stress, now directly backing round-12's central external dependency)** stay valid. WP2/WP10–WP13 are historical mechanism checks (no-automata certificate machinery; round-11's product-state/transformer representation, which round-12 deletes); `wp13_referee_acceptance_tests.py`'s acceptance-test *verdicts* (`C_force`/`C_split` UNSAT, `C_recursive`/`C_↑` SAT) remain the correct spec — re-verified by the 6th review (its S4) and still decided correctly by the cover-tree tableau — but the product-state *mechanism* wp13 modeled is superseded by patchwork bags. **WP14–WP16 (July 2026, from the 7th review) are the first artifacts probing the patchwork-bag layer itself**: WP14 verifies the round-12 patchwork theorem (Thm 2.5 / App A) directly at small widths; WP15 machine-checks the Shadow Amalgamation gap and its repair disciplines; WP16 cross-validates the acceptance mechanism against the cover-tree tableau. **WP17 (round-13) machine-checks the Shadow Amalgamation Lemma's finite facts and constructive content.** Still unverified: the round-13 completeness side (Coverage Lemma, extraction-within-discipline, verticalization-vs-width) and an actual two-way parity-automaton emptiness run.

The no-automata thread (rounds 2–10) is retained as the historical audit trail, **not retracted as wrong** — its repeatedly-rediscovered keystones (finite checkability, simultaneous eventuality fulfillment) are exactly what the automaton discharges by construction. See `OUTDATED.md` ("The no-automata split-forest thread") for the full progression: round-7 incidence-tag fix (the round-6 `L_Q(π,π)=EQ` lap-collapse defect); round-8 saturated side contexts; the cold Opus 4.8 review finding D-1 (the ancestor-tower gap); round-9 forced verticalization closing D-1; the round-9 cold review finding the dual descendant case + the two foundational gaps; round-10's product-state-exhaustion repair. Manuscripts: `papers/gpt5.5_round2/` (rounds 4–6), `papers/gpt5.5_final/repaired_split_forest_all_in_one_round7.*` + `expanded_split_forest_full_details_proof.*` (round 7), `papers/claude_latest_review_gpt5.5_fix/` (round 8), `papers/gpt5.5_round3/` (round 9), `papers/gpt5.5_round4/` (round 10).

**Verification scripts.** Claude's role is verification + audit + documentation. Self-contained Python verification scripts under `verification/python/`:
- `wp7_selfcontained_side_witness.py` -- comparable side witnesses stress
- `wp8_round7_blocking_chain.py` -- blocking-not-equality stress (round-7 central repair, still load-bearing in round-8/9)
- `wp9_round7_split_copies.py` -- split copies for incomparable proper superparts
- `wp10_round9_forced_verticalization.py` -- round-9 forced-verticalization certificate-**emission** test: drives `C0'` end-to-end through DETECT (forced ancestor trace + threshold) → SPLICE (derive, not hand-place, the `E_up` edge) → BUILD (tagged certificate, tower regularized to a request-closed cycle) → VALIDATE (round-9 clauses: converse, V6 composition, universal safety, V9 equality, request discharge, residual-frontier ⊆ {DR,PO}). Emits a VALID certificate for `C0'` and REJECTS the UNSAT sibling (all routings fail). Negative-controlled (the validator catches the D-1 residual-frontier violation when the splice is withheld).
- `wp11_general_forced_verticalization.py` -- **general** forced-verticalization engine + oracle cross-check: generalizes WP10 from the single `C0'` witness to a parameterized family (period-p tower profiles, many side witnesses with arbitrary source relations and negative-literal payloads, universals over {PO,PPI,DR,PP}). For each spec it runs the full DETECT→ROUTE→BUILD→VALIDATE pipeline and cross-checks the emit/REJECT verdict three ways: against a by-construction oracle (`family_status`), against the independent validator on the concrete certificate, and (period-1, finitely encodable, closure ≤ 20) against the cover-tree tableau decision procedure. Result on the current sweep (216 specs): **216/216** engine-vs-by-construction agreement, **0** invalid certificates, **32/32** tableau confirmations — i.e. no round-9 completeness/soundness gap found on this family. Honest scope: it does NOT yet stress the hardest part of obligation D — witnesses that are proper parts of *each other* or that share superparts (overlap amalgams), where simultaneous splices could genuinely interact; the splice pass is written as a fixpoint so that extension is structural. The harness has teeth: it caught a real composition bug in its own inter-witness labeling during development, and the negative control catches D-1.
- `wp12_round10_dual_descendant_verticalization.py` -- machine-checks the round-9 **cold review's Critical-1 dual case** (the descendant PP-tower the ancestor-only round-9 missed) and round-10's dual-splice repair. Mirror of WP10: drives `C = (∃PO.A) ⊓ (∃PPI.⊤) ⊓ ∀PPI.(∃PPI.⊤ ⊓ ∀DR.¬A ⊓ ∀PO.¬A)` through DETECT (descendant trace `comp(PP,·)`, shape DR\*PO\*PP\*, PP absorbing) → SPLICE (splice `w` as the descendant-tower superpart, `d_{k0} PP w`) → BUILD → VALIDATE. Emits VALID for `C`, REJECTs the UNSAT sibling (add `∀PP.B`, `w∈¬B`), negative-controlled (catches the dual-D-1 residual-frontier violation). Confirms Critical-1's repair is sound at the emission level — but says nothing about round-10's two unproven keystones (product exhaustion, complete-interface), which are not machine-checkable this way.
- `wp13_referee_acceptance_tests.py` -- **automata-route (Theorem B) mechanism check.** Models the repaired proof's product-state representation as composition closure (path-consistency) + universal-aware + type-aware-EQ domain pruning + request-closed blocking, and runs the 5th cold review's four acceptance tests (report §9): `C_force = ∃PP.(∃DR.A) ⊓ ∀DR.¬A` → REJECT (forces `x DR z`, kills `z:A` — where the old `Patch` wrongly accepted); `C_split` → REJECT; `C_recursive = ∃DR.(A ⊓ ∃DR.B)` → ACCEPT, and REJECT if the recursive B-witness is omitted (recursive child-obligation check); `C_↑` → ACCEPT via a PP-labelled (not EQ) repeated lap. Each verdict cross-checked against the independent cover-tree tableau — all four agree. Checks Theorem B's *mechanism* on these witnesses; does NOT verify its general finiteness/adequacy (the manuscript-level keystone awaiting cold review).
- `wp14_patchwork_property_check.py` -- **round-12 patchwork theorem (Thm 2.5 / App A) direct verification** (7th review). For abstract composition-table semantics, "satisfiable complete atomic network" = "composition-closed network", so the proof's central external dependency is machine-checkable: T0 comp table from set semantics; T1 two-bag patchwork exhaustive at 1+1 fresh vars, shared parts 0–3 (21,538 agreeing pairs); T2 two-bag at 2+2 fresh (41,681 pairs); T3 300 random tree-shaped bag families amalgamated leaf-by-leaf literally per App A's induction; T4 EQ-forcing impossibility (every nontrivial comp cell containing EQ also contains PP, PPI, PO — backs App G). All PASS, zero failures; a failure would invalidate the proof (App U, F1).
- `wp15_shadow_row_realizability.py` -- **the 7th review's critical finding (F1) machine-checked.** Models round-12's shadow legality tests literally (per-bag one-point closure, boundary rigidity, non-EQ entries) and demonstrates the Truth Lemma's implicit Shadow Amalgamation assumption is false as written: P1 minimal 3-bag configuration passes every listed test, no realizing frame; P2 exhaustive: 23/64 pattern assignments unrealizable. Repair map corroborated: P3 single shadow always realizable (250/250 — provable, agreeing-tree-family argument); P4 DR/PO-only entries (vertical-liveness discipline) 358/358 clean; P5 extended-bag discipline (per-bag shadow-shadow checks) 200/200 clean. A passing run CONFIRMS the defect + repair map (it is an attack harness, not a proof check).
- `wp16_round12_bag_acceptance.py` -- **acceptance-level patchwork-bag mechanism check vs the cover-tree tableau** (7th review). Finite-occurrence rendering of round-12 acceptance semantics (Hintikka types, witness edges with exact-occurrence-identity reuse, request-closed regularity, patchwork completion of undeclared pairs). Repaired mode (obligations on actual completed relations; no EQ entries; mandatory propagation): matches the tableau on all ten diagnostics — `C_force`/`C_split`/`C_2hop`/`C_3hop` UNSAT; `C_recursive`/`C_↑`/dual-descendant tower/**PO-loop** SAT (the quasimodel oracle's blind spot, decided correctly here) — plus a 148-concept random sweep, zero disagreements. Literal mode (either spec loophole F2/F3 active): wrongly ACCEPTS all four forced-composition UNSAT concepts. Bounded probe (occurrence budget; request-closed blocking), not a Theorem-B verification.
- `wp17_round13_discipline_check.py` -- **round-13 Shadow Amalgamation repair, machine-checked.** Part A: the four finite composition facts of the round-13 proof, exhaustive against an independently re-derived table (A1 horizontal absorption `{DR,PO} ⊆ comp(a,b)` for horizontal a,b; A2 vertical transitivity; A3 no horizontal dead ends; A4 EQ never forced). Part B: the lemma's constructive assignment (bags + live rows + shadow rows + unique pair values + DR-default) on 400 random **tree-shaped** configurations under the D-discipline: B1 extended-bag closure 400/400, B2 global joint realizability 400/400. Part C negative control: vertical entries re-admitted ⟹ 151/300 fail (the WP15 defect the discipline excludes). Development note: an earlier Part B failed 143/400 because two shadows could co-locate without a declared mutual value — the observation that forced pair-coherence rule D5 into the round-13 manuscript.
- `wp18_g2a_verticalization_width_counter.py` / `wp19_g3_mixed_orientation_probe.py` -- **the 8th review's counterexamples** (GPT-5.5): the G2a tower's unbounded root-bag width under round-13's literal D2 (oracle-SAT check + prefix-closure + fallback count), and the mixed `PP/PPI` closed path with vertical endpoint that D3 never reaches. Both reproduced locally.
- `wp26_round15_cluster_quasimodels.py` -- **round-15 cluster-quasimodel decision layer, machine-checked** (Claude). Part A: the **fold lattice** — exactly 10 sets arise as path-compositions of RCC5 atoms; every non-singleton fold contains a horizontal; the three DR-free folds contain PO (canonical selector total); plus the steering toolkit — all exhaustive. Part B (keystone W1 probe): 250/250 random cluster-tree glue steps jointly realizable with canonical-selector domains; negative control 245/249 fail when horizontals are stripped. Part C: acceptance semantics vs cover-tree tableau — 11/11 diagnostics (incl. `C_G2a`, the 9th review's p4 tower) + 200-concept sweep, 0 disagreements, 0 timeouts.
- `wp21_twin_blowup_facts.py` / `wp22_g2a_profile_bound.py` / `wp23_active_closure_regressions.py` / `wp24_vertical_walk_facts.py` / `wp25_active_virtual_bag_repair.py` -- **round-14 (active-virtual-bag) checks** (GPT-5.5, reproduced locally): same-profile copy chains/cliques facts; G2a profile-count boundedness; regressions (no DR-default; WP15/WP20-style shadow-shadow triangles rejected by active closure); uniform-vs-mixed vertical walk facts; and the main repair check (G2a absorbed: old fallback unbounded vs. 2 live ports + virtual shadows; bad triangles rejected; random active restrictions closed). All PASS.

NOTE: WP10–WP12 verify the *no-automata* thread's certificate machinery (now the historical thread); WP13 exercises the automata route's Theorem-B mechanism on the referee's tests. WP1 + the SAT/UNSAT oracles + WP7–WP9 (Theorem A stress) remain valid for the current (automata) statement. No artifact yet checks Theorem B's general finiteness/adequacy or a full parity-tree-automaton non-emptiness procedure.

All pass. They corroborate the central stress concepts that round-7 introduced and round-8/9 preserve. WP10 fulfils the round-8 review's recommendation to add `C0'` (and its UNSAT sibling) as a constructive certificate-emission test, exercising the round-9 infinite-tower/forced-verticalization machinery that wp7-9 did not; WP11 generalizes that test across the forced-verticalization axes with an independent oracle cross-check. The D-1 composition arithmetic and the proposed-repair certificates are machine-checked under `papers/opus4.8_review/` (`rcc5_compose.py`, `fix_feasibility.py`, `repaired_certificate.py`).

Claude's earlier completeness-extraction papers (`papers/completeness_extraction_ALCIRCC5.tex` = v1, and `papers/completeness_extraction_ALCIRCC5_v2.tex` = v2.1) are **superseded** by GPT-5.5's round-8 proof and kept for historical reference. Do not cite them as the current completeness pillar.

**No complexity bound is asserted for the full logic.** Plain ALCI is EXPTIME-complete (known). ALCI_RCC8 is EXPTIME-hard (Wessel lower bound). The PO-coherent fragment's quotient bound is 2-EXPTIME (`papers/two_tier_quotient_ALCIRCC5.tex`). The round-7 / round-8 proof commits only to an effectively computable bound on the certificate space. Do not reintroduce an EXPTIME upper-bound claim for the full logic -- that was tied to the retracted quasimodel paper.

Both directions rely on the **patchwork property** of RCC5/RCC8 (Renz & Nebel 1999): path-consistent atomic constraint networks are globally consistent.

## Computational Verification (WP1-WP6)

Six Python work packages under `verification/python/` cross-check the finite combinatorial content of the repaired proof. All six **PASS**, no counterexample found:

- WP1: RCC5 composition table by exhaustive subset enumeration
- WP2: bounded certificate-soundness fuzzing for (V1)/(V3)/(V4)/(V5)/(V9)/(V10)
- WP3: small-model SAT/UNSAT oracle
- WP4: multiple-superpart C_AB stress (folded into WP2/WP3)
- WP5: request-closed blocking-cycle tests (folded into WP2)
- WP6: mosaic closure search (M1)-(M5) and (SC1)-(SC4)

## Retracted / Superseded

- `papers/decidability_ALCIRCC5.tex` -- **Retracted.** Earlier quasimodel + type-elimination approach; type elimination's Q3 anti-monotonicity caused cascade elimination (incompleteness, not unsoundness). Kept in the repo for historical reference.
- `papers/completeness_extraction_ALCIRCC5.tex` (v1) and `papers/completeness_extraction_ALCIRCC5_v2.tex` (v2.1) -- **Superseded** by GPT-5.5's repaired proof after the round-2 review found two structural defects in v1 and a residual omega-acceptance dependency in v2.1.
- The Python quasimodel reasoner `src/alcircc5_reasoner.py` is retained only as a cross-validation oracle. It is **known-incomplete** on the PO-loop pattern (e.g., `C ⊓ ∃PO.∃PO.C ⊓ ∀{PO,DR,PP,PPI}.¬C`, SAT via a 2-element symmetric loop under strong EQ identity). **SAT answers are trustworthy; UNSAT answers on cyclic-via-symmetric-role concepts are not.** The 911-concept "zero mismatches" cross-validation held because the test set did not include the blind-spot pattern.

## Key Files

- `papers/automata_route_repairs/split_forest_automata_with_appendices.tex` -- **the canonical decidability statement** (round-12 *expanded / self-contained edition*, 29pp, compiles clean, 0 undefined refs). **Same theorem spine as the 14pp round-12** (split-forest normal form = Theorem A → **patchwork-bag** finite abstraction = Theorem B → **two-way** alternating parity automaton correctness = Theorem C → Decidability), now with 21 appendices (A–U) discharging every load-bearing step in full: witness thinning (App B/L), saturated side contexts + the effective `K(C_0)` width bound (App C), the **finite-to-countable patchwork passage** (App A: finite tree-patching by induction on bags + a **compactness argument over the finite relation alphabet** for the countable completion), the Theorem-B realization + truth lemma (App E/M), relation-vector shadows (App F), exact occurrence identity / running-intersection replacing the EQ quotient (App G/R), automaton states + parity (App H/N), effective bounds (App I), an implementation-ready decision procedure + local legality tests (App K/Q), diagnostic examples incl. the `PPI;PP` DR-exclusion (App O), the dependency graph (App T), and "what would invalidate the proof" (App U). Crucially this is **not a new repair round** — it is the same mathematics made self-contained; its global-coherence argument (App A+E) derives `ρ_T` from compactness + finite patchwork, with **no route-dependent transformer**, which is the route the 6th cold review recommended (it did not introduce the transformer-coherence layer the 6th review faulted). Still **NOT cold-reviewed in turn**; still depends on the **same two standard external theorems** (RCC5 patchwork property; two-way parity-automaton emptiness, Vardi); keystone sharpened by the 7th review (hot, 2026-07-08) to the **Shadow Amalgamation Lemma + K(C_0) accounting** (see `papers/fable5_review/`). Status unchanged: strongly supported, not certified.
- `papers/fable5_review/` -- **the 7th review** (2026-07-08, Claude Fable 5, *hot* audit — does not discharge the cold-review requirement): `referee_report_round12_fable5.tex/.pdf` (9pp; findings F1 Shadow Amalgamation, F2 EQ-entry loophole, F3 shadow-scope loophole, F4 K-bound accounting, minors; sound-steps list; WP14–WP16 results; repair program for round 13) and `fable5_review_round12_bundle.zip` (self-contained response bundle for GPT-5.5 without repo access; probe output logs live in `verification/reports/`).
- `papers/fable5_round13/split_forest_shadow_amalgamation_round13.tex` -- **the round-13 repair delta** (2026-07-08, 10pp, compiles clean, 0 undefined refs). Together with the round-12 base it is the current **candidate** statement: the D-discipline (D1–D5) + the **Shadow Amalgamation Lemma** (proved from four machine-checked finite facts + patchwork/compactness on the shadow-extended family) + the repaired Truth Lemma (declared entries literally equal `ρ_T`) + the Coverage Lemma (completeness side) + the witness-generated `K(C₀)` accounting (F4 fix) + the automaton delta (mandatory propagation, no-EQ entries, per-request parity priorities, arity padding). **Reviewer-authored, UNREVIEWED** — its own §"what would invalidate" designates the extraction-within-discipline and Coverage-walk steps (G2/G3) as the next review targets. Machine checks: WP17. A drag-and-drop **G2/G3 task packet for GPT-5.5** is at `papers/fable5_round13/gpt_task_g2_g3_round13.zip` (+ unpacked `gpt_task_g2_g3/`): targeted attack-and-discharge prompt, an optional cold-ish first-pass prompt (attach only the two manuscripts), both manuscripts, the 7th review, WP14–WP17 + logs, and the tableau oracle. The overview papers carry a "Round 13 (July 2026, unreviewed)" paragraph in their July-2026 update subsection.
- `papers/automata_route_repairs/split_forest_patchwork_automata_repaired.tex` -- the 14pp round-12 **theorem-spine** version (no appendices). Same A/B/C/Decidability content; superseded as the citable manuscript by the 29pp expanded edition above. The 6th cold review that prompted round-12: `referee_report_ALCI_RCC5.md`; GPT-5.5's response: `formal_response_referee_round12.tex`.
- `papers/automata_route_repairs/split_forest_automata_repaired_full_proof.tex` + `split_forest_companion_ABC_paper.tex` -- the 5th-review repair (19pp + A/B/C companion, 18pp, Theorem A/B/C). **Superseded by round-12** after the 6th cold review (`rcc5_split_forest_referee_report.tex` was the 5th review) found Theorem B's interface-transformer coherence asserted-not-proved.
- `papers/gpt5.5_final/split_forest_automata_decidability_proof_detailed.tex` -- the original (21-page) automata proof, **superseded** (5th cold review found its `Patch` invariant assumed-not-constructed).
- `papers/overview_ALCIRCC5.tex` -- Self-contained overview paper. Primary entry point for the argument; cites the v1 split-forest paper and (as of 2026-05-29) the automata proof as the core decidability pillar, with the cover-tree tableau papers as the operational layer.
- `papers/dl2026_abstract_ALCIRCC5.tex` -- DL 2026 abstract, parallel content to the overview.
- `papers/gpt5.5_round4/` -- round-10 no-automata manuscripts + the round-9 cold-review referee report (`referee_response_alci_rcc5.*`) + GPT-5.5's response (`formal_response_referee_round10.*`). Historical thread; `expanded_split_forest_full_details_round10_merged.*` is the full round-10 expanded proof (the `_corrected` files are a lossy delta).
- `papers/cold_review_round9/` -- drag-and-drop cold-review packet (neutralized PDFs + prompt + withhold-list). Repurposable for cold-reviewing the automata proof.
- `papers/sibling_interface_descriptors_ALCIRCC5_v2.tex` -- v2 sibling-interface delta paper (fixes v1 Def 1.5, 1.7, Thm 1.20-sketch).
- `papers/cover_tree_tableau_ALCIRCC5.tex` -- Claude's cover-tree tableau implementation paper (9 pages).
- `papers/two_tier_quotient_ALCIRCC5.tex` -- PO-coherent fragment decidability proof (12 pages).
- `papers/trees/` -- GPT-5.4's v1 split-forest and cover-tree tableau theory papers.
- `papers/LRCC8_vs_ALCIRCC8.tex` -- Why the Lutz-Wolter undecidability of L_RCC8 does not transfer to ALCI_RCC8; documents priority claims relative to Wessel's 2002/2003 report.
- `src/cover_tree_tableau.py` -- Cover-tree tableau implementation (~350 lines).
- `src/alcircc5_reasoner.py` -- Quasimodel-based reasoner (cross-validation oracle; see caveat above).
- `src/decomposition_test.py` -- Cover-tree decomposition test (775/775 = 100%).
- `src/stress_test_cover_tree.py` -- Cross-validation suite (911 concepts, 0 mismatches post-fix).
- `src/gis_taxonomy.py` -- GIS taxonomy computation (18 concepts, 21/21 subsumptions from report7.pdf).
- `README.md` -- Full project description with complexity landscape and all approach summaries.
- `CONVERSATION.md` -- Full conversation log. **Before modifying settled topics (citations, decidability-argument wording, EXPTIME claims, Lutz-Wolter priority), check CONVERSATION.md for the relevant audit section.** Citations were verified in April 2026.

## Technical Conventions

- Concepts are in **negation normal form** (NNF); inverse roles are absorbed (exists PP^-.C = exists PPI.C).
- **Strong EQ semantics** throughout (EQ = identity). Weak EQ reduces to strong via TBox internalization.
- The 5 base relations are DR, PO, EQ, PP, PPI. For edges between distinct nodes, only {DR, PO, PP, PPI} are used (EQ is reflexive only).
- LaTeX macros: `\ALCIRCC{5}`, `\DR`, `\PO`, `\PP`, `\PPI`, `\EQ`, `\comp`, `\cl`, `\Tp`, `\Pair`, `\Trip`, `\EXPTIME`, `\PSPACE`.
- **Terminology.** Canonical name for the semantic framework is "split-forest model" (not "split-tree forest model"). The formal data object is a "weak-EQ split-tree presentation": one tree per connected component, produced by splitting join nodes into EQ-mates. A "split-forest" is the collection of such split-trees.
