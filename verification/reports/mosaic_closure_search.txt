===============================================================================
WP6: mosaic closure counterexample search
===============================================================================

>>> Test A: partition 3-position networks by (M3) consistency
    Enumerating 4^3 = 64 ordered (L(0,1), L(0,2), L(1,2)) triples
    over off-diagonal RCC5 base relations.

    41/64 triangles are (M3)-consistent.
    23/64 triangles violate (M3) (correctly rejected by check_M3).
    PASS: every (M3)-rejected triangle is also unrealizable by finite sets.
          (M3) is exact, not over-approximating.

>>> Test B: locally consistent triangles are RCC5-realizable
    For every (M3)-consistent triangle, find finite subsets of {0..n-1}
    realizing the labels (Renz-Nebel patchwork property base case).

    PASS: every locally-consistent triangle is realizable for n<=5.
    (Confirms patchwork property on triangles for RCC5.)

>>> Test C: DR/PO side-witness insertion
    For exists R.B with R in {DR, PO} and consistent universals at p,
    confirm we can choose tau(q) satisfying (M4) at both ends.

    PASS: DR/PO witnesses inserted without (M4) violation in both directions.

>>> Test D: universal propagation through PP-chain
    forall PP.A at p, L(p,q)=PP, L(q,r)=PP forces L(p,r)=PP, so A in tau(r).

    PASS: legal PP-chain accepted; broken PP-chain caught by (M4).

>>> Test E: sibling branching (PP-child of PO-related sibling)
    p PPI s1, p PPI s2, s1 PO s2, s1 PPI c.  Which L(c, s2) are legal?
    Composition: comp(PP, PO) = {DR, PO, PP}.

    PASS: legal L(c, s2) candidates = ['DR', 'PO', 'PP']
    Matches comp(PP, PO) = ['DR', 'PO', 'PP'] exactly.

>>> Test F: arity-4 atomic 4-networks (path-consistency vs realizability)
    Enumerate all 4^6 = 4096 atomic labelings of 4 positions, partition
    by (M3) on every triple, then realize each (M3)-consistent labeling
    by 4 nonempty finite subsets (Renz-Nebel patchwork at arity 4).

    916/4096 4-networks are (M3)-consistent.
    3180/4096 violate (M3) on some triple.
    NOTE: 7 (M3)-consistent 4-networks need n>5 to realize.
          (Pattern: 3 pairwise-DR positions all PO a fourth.)
    PASS: every (M3)-consistent 4-network is realizable for n<=6.
    (Confirms patchwork property at arity 4 for atomic RCC5.)

>>> Test G: overlap amalgamation of two arity-3 mosaics
    Two triangles M1=(a,b,c) and M2=(b,c,d) sharing edge {b,c}.
    For every (M3)-consistent pair, find atomic L(a,d) making the
    joint 4-mosaic (M3)-consistent (the patchwork lemma at arity 4).

    427/427 overlap-compatible triangle pairs amalgamate.
    PASS: every overlap-compatible consistent pair admits an extension.
    (Confirms patchwork: local agreement -> global synchronization.)

===============================================================================
VERDICT: PASS.  Support-closed mosaics give global triple composition
         on every test we ran; DR/PO side witnesses are insertable;
         arity-4 patchwork holds (Tests F and G).
         No counterexample to the patchwork lemma was found.
